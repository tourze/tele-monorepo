# VibeShell

基于 Nx 的开发环境，用于管理多个 Gitee 项目。

## 克隆项目

```bash
# 克隆所有配置的项目
npm run gitee:clone

# 直接克隆指定仓库到apps目录
npm run gitee:clone https://gitee.com/umworks/utc-react.git
```

## 运行项目

进入 `apps/` 目录下的具体项目，安装依赖并运行：

```bash
# 示例：进入项目并运行
cd apps/your-project-name

# 安装依赖
npm install
# 或
yarn install
# 注意：所有子仓库都不要使用pnpm安装

# 启动项目
npm run dev
# 或
npm start
```

⚠️ **注意**：所有子仓库都必须使用 `npm install` 或 `yarn install`，不要使用pnpm，否则会出现模块解析错误。

## 子仓库分支管理

### 切换分支

```bash
# 进入子项目目录
cd apps/your-project-name

# 查看所有分支
git branch -a

# 切换到指定分支
git checkout branch-name

# 创建并切换到新分支
git checkout -b new-branch-name
```

### 提交子仓库更新

子仓库的更新需要自行推送到各自的 Gitee 仓库：

```bash
# 进入子项目目录
cd apps/your-project-name

# 查看变更
git status

# 添加文件并提交
git add .
git commit -m "your commit message"

# 推送到 Gitee 仓库
git push

# 推送指定分支
git push origin branch-name
```

## 🧠 OpenSpec - 规范驱动开发

使用 OpenSpec 让 AI 严格按照规范工作，减少幻觉。

### ⚡ 快速开始

| 步骤 | 命令/操作 | 说明 |
|------|-----------|------|
| 1️⃣ 检查规范 | `openspec list --specs` | 查看所有可用规范 |
| 2️⃣ 下达任务 | 参考下方模板 | 给AI明确的规范约束 |
| 3️⃣ 验证结果 | `openspec validate` | 检查是否符合规范 |

### 📋 AI任务模板

```bash
请严格按照以下规范执行任务：

📋 **任务**: [具体任务描述]
📚 **遵循规范**: openspec/specs/react-standards/spec.md
🎯 **具体要求**:
- 引用具体规范条款
- 明确文件路径和项目名
- 指定技术栈要求

请逐项确认，不允许偏离规范。
```

### 🛠️ 常用命令

| 类别 | 命令 | 功能 |
|------|------|------|
| **查看** | `openspec show react-standards` | 查看React规范 |
| **提案** | `openspec validate <change>` | 验证提案 |
| **归档** | `openspec archive <change> --yes` | 归档完成提案 |
| **全部** | `openspec validate --all` | 验证所有规范 |

### 🔥 常用规范

- **React 组件开发**: `openspec show react-standards`
- **项目结构管理**: `openspec show nx-workflow`
- **项目技术栈**: 查看 `openspec/project.md`

### 📝 创建功能提案

| 类型 | 创建命令 | 归档命令 |
|------|----------|----------|
| **项目特定** | `mkdir -p openspec/changes/项目名/功能名称` | `openspec archive 项目名/功能名称 --yes` |
| **工作区级别** | `mkdir -p openspec/changes/功能名称` | `openspec archive 功能名称 --yes` |

**必要文件结构**:
```
openspec/changes/[项目名/]功能名称/
├── proposal.md     # 为什么要做这个功能
├── tasks.md        # 具体要做哪些事
└── specs/          # 规范变更内容
    └── [capability]/spec.md
```

**提案模板**:
- **proposal.md**: Why (原因) + What Changes (变更) + Impact (影响)
- **tasks.md**: 功能开发 → 测试验证 → 归档步骤

### 📁 归档机制

- ✅ **推荐**: `openspec archive <change> --yes` (自动合并规范)
- ⚠️ **备选**: 手动 `mv` (可能遗漏规范更新)
- 📂 **结果**: `openspec/archive/[项目名/]YYYY-MM-DD-功能名称/`

### 🚨 重要提醒

- **AI 不听话？** → 引用具体规范条款
- **想添加新功能？** → 按模板创建提案
- **代码不符合规范？** → `openspec validate --all`
- **高级用法？** → 查看 `openspec/ADVANCED.md`

### 🛠️ 项目启动
```bash
# UTC 项目 (React 17 + TypeScript)
npm run utc-react:dev

# Seven Fish 项目 (React 16 + JavaScript)
npm run seven-fish:start
```

---

**项目配置在 `gitee-projects.json` 中**