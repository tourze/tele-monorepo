# VibeShell

基于 Nx 的开发环境，用于管理多个 Gitee 项目。

## 克隆项目

```bash
# 克隆所有配置的项目
npm run gitee:clone

# 直接克隆指定仓库到apps目录
npm run gitee:clone https://gitee.com/umworks/utc-react.git
```

## 运行项目

进入 `apps/` 目录下的具体项目，安装依赖并运行：

```bash
# 示例：进入项目并运行
cd apps/your-project-name

# 安装依赖
npm install
# 或
yarn install
# 注意：所有子仓库都不要使用pnpm安装

# 启动项目
npm run dev
# 或
npm start
```

⚠️ **注意**：所有子仓库都必须使用 `npm install` 或 `yarn install`，不要使用pnpm，否则会出现模块解析错误。

## 子仓库分支管理

### 切换分支

```bash
# 进入子项目目录
cd apps/your-project-name

# 查看所有分支
git branch -a

# 切换到指定分支
git checkout branch-name

# 创建并切换到新分支
git checkout -b new-branch-name
```

### 提交子仓库更新

子仓库的更新需要自行推送到各自的 Gitee 仓库：

```bash
# 进入子项目目录
cd apps/your-project-name

# 查看变更
git status

# 添加文件并提交
git add .
git commit -m "your commit message"

# 推送到 Gitee 仓库
git push

# 推送指定分支
git push origin branch-name
```

## 🧠 OpenSpec - 锁定 AI 意图，减少幻觉

项目使用 OpenSpec 规范驱动开发，让 AI 严格按照你的要求工作，不再胡乱发挥。

### 🎯 给 AI 下达任务的正确方式

#### ✅ **错误方式** (容易产生幻觉)
```
"帮我优化一下用户登录功能"
```

#### ✅ **正确方式** (严格按照规范)
```
"请根据 openspec/specs/react-standards/spec.md 中的「函数组件开发标准」和「性能优化规范」，
优化 apps/utc-react/src/pages/Login/index.tsx 中的用户登录功能。
要求：
1. 使用函数组件 + Hooks 模式
2. 遵循 Props 类型定义规范
3. 添加 React.memo 优化
4. 使用 callAPI 统一调用模式"
```

### ⚡ 快速开始 (3步搞定)

#### 1️⃣ 检查规范
```bash
openspec list --specs  # 查看所有规范
openspec show react-standards  # 查看React开发规范
```

#### 2️⃣ 给AI下达任务 (复制这个模板)
```
请严格按照以下规范执行任务：

📋 **任务**: [这里写你的任务]
📚 **遵循规范**: openspec/specs/react-standards/spec.md
🎯 **具体要求**:
- 引用具体的规范条款
- 提及文件路径和项目名称
- 明确技术栈要求

请逐项确认，不允许偏离规范。"
```

#### 3️⃣ 验证结果
```bash
npm run openspec:validate  # 检查是否符合规范
```

### 🔥 最常用的规范

- **React 组件开发**: `openspec show react-standards`
- **项目结构管理**: `openspec show nx-workflow`
- **项目技术栈**: 查看 `openspec/project.md`

### 📝 如何创建功能提案

当需要添加新功能或重大变更时，创建提案让 AI 严格按照规范执行：

#### 🛠️ 手动创建提案 (3步)

**1️⃣ 创建提案目录**
```bash
mkdir -p openspec/changes/你的功能名称
```

**2️⃣ 创建3个必要文件**
```bash
# 提案说明 (为什么要做这个功能)
touch openspec/changes/你的功能名称/proposal.md

# 任务清单 (具体要做哪些事)
touch openspec/changes/你的功能名称/tasks.md

# 规范变更 (需要修改哪些规范)
mkdir -p openspec/changes/你的功能名称/specs/react-standards
touch openspec/changes/你的功能名称/specs/react-standards/spec.md
```

**3️⃣ 验证提案**
```bash
openspec validate 你的功能名称
```

#### 📋 提案模板 (复制使用)

**proposal.md**:
```markdown
## Why
[这里写为什么要做这个功能，解决什么问题]

## What Changes
- [具体要做的变更1]
- [具体要做的变更2]
**BREAKING**: [如果有破坏性变更]

## Impact
- Affected specs: [影响的规范]
- Affected code: [影响的代码文件]
```

**tasks.md**:
```markdown
## 1. 功能开发
- [ ] 1.1 具体任务1
- [ ] 1.2 具体任务2

## 2. 测试验证
- [ ] 2.1 测试功能
- [ ] 2.2 openspec validate 检查
```

### 🚨 重要提醒

- **AI 不听话？** → 引用具体的规范条款
- **想添加新功能？** → 按上面模板创建提案
- **代码不符合规范？** → `openspec validate --all` 检查

### 🛠️ 项目启动
```bash
# UTC 项目 (React 17 + TypeScript)
npm run utc-react:dev

# Seven Fish 项目 (React 16 + JavaScript)
npm run seven-fish:start
```

---

**项目配置在 `gitee-projects.json` 中**