# Vibe-Shell OpenSpec 配置总结

## 配置概览

仓库已经完成以下基础设施：

- 使用 Yarn Workspaces 管理 `apps/*` 与 `packages/*` 两类工作区；
- OpenSpec 目录结构（`openspec/`）已就绪，可支撑规范驱动的开发流程；
- 提供自定义校验脚本 `scripts/openspec-validate.js`，用于检查仓库配置与规范文件；
- `package.json` 中预置了一系列 Yarn 脚本，便于安装依赖、同步项目与执行校验。

## 核心配置文件

### `package.json`
- ✅ 启用了 `workspaces`，覆盖 `apps/*` 与 `packages/*`
- ✅ 所有脚本统一使用 Yarn，包括 `clone-project`、`sync-project`、`openspec:validate` 等
- ✅ `reinstall` 会清理多级 `node_modules` 与锁文件后重新执行 `yarn install`

### `openspec/`
- ✅ `AGENTS.md` 与 `project.md` 描述仓库背景与协作约定
- ✅ `specs/`、`changes/` 等目录结构完整，便于扩展团队规范

### `scripts/openspec-validate.js`
- ✅ 检查 OpenSpec 目录结构是否完整
- ✅ 检查每个工作区项目是否提供 `.openspec` 描述
- ✅ 校验根目录 `package.json` 中的 Workspaces 配置
- ✅ 如环境安装了 OpenSpec CLI，会尝试执行 `openspec validate --all`

## 常用命令

```bash
# 安装依赖（首次或依赖变更后）
yarn install

# 校验 OpenSpec 配置与工作区状态
yarn openspec:validate

# 列出所有工作区及其状态
yarn list-projects

# 克隆或更新 apps 目录中的业务项目
yarn clone-project <name> <git_url>
yarn update-project <name>
yarn sync-project
```

## 开发建议

1. **统一使用 Yarn**：避免混用 `npm` 或 `pnpm`，确保锁文件、依赖版本一致。
2. **维护 `.openspec`**：每个工作区建议维护 `.openspec`，记录项目背景及适用规范。
3. **结合 OpenSpec CLI**：如需更细致的规范校验，可全局安装 `openspec` 并结合 CI 使用。
4. **及时更新工作区脚本**：新增业务项目时，记得在 `apps/` 中添加对应目录与脚本。

## 下一步

- 在 CI/CD 流程中加入 `yarn openspec:validate`，保证规范要求；
- 根据团队需求补充 `openspec/specs/` 内的规范文档；
- 为常用工作区补充更多 Yarn 脚本（如测试、构建、发布等）。

---

配置已经完成，欢迎在此基础上继续扩展规范与工具链。🎉
